name: CI pipeline

on:
  push: 
    branches: [ main ]
  pull_request: 
    branches: [ main ]

jobs:
  display-file:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4 
        
      - name: Print the file content
        run: cat info.txt

